import React, { Component, ReactNode } from 'react';
import { Text } from 'react-native';

class AIScreen extends Component {
    public render(): ReactNode {
        return (
            <Text>AI!</Text>
        );
    }
}

export default AIScreen;
